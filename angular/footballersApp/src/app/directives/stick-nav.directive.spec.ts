import { StickNavDirective } from './stick-nav.directive';

describe('StickNavDirective', () => {
  it('should create an instance', () => {
    const directive = new StickNavDirective();
    expect(directive).toBeTruthy();
  });
});
